
/*
import React, { useState } from 'react';
import { 
  Card, 
  CardContent, 
  CardDescription, 
  CardHeader, 
  CardTitle
} from '@/components/ui/card';
import { 
  Tabs, 
  TabsContent, 
  TabsList, 
  TabsTrigger 
} from '@/components/ui/tabs';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { 
  Award, 
  BarChart4, 
  Image as ImageIcon, 
  DownloadCloud,
  Activity,
  LineChart,
  Table as TableIcon,
  FileText
} from 'lucide-react';
import { 
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow
} from '@/components/ui/table';
import { Dialog, DialogContent, DialogTrigger } from '@/components/ui/dialog';
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';
import { ExperimentResults } from '@/types/training';
import ClassificationReportTable from './ClassificationReportTable';

// This component is currently commented out as requested by the user
// It will be revived later when needed

interface CustomTrainingResultsProps {
  experimentResults: ExperimentResults;
  onReset: () => void;
}

const CustomTrainingResults: React.FC<CustomTrainingResultsProps> = ({ 
  experimentResults, 
  onReset 
}) => {
  return null;
};

export default CustomTrainingResults;
*/
