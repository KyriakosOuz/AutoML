
// Import from the hook implementation
import { useToast, toast, type ToastOptions } from "@/hooks/use-toast";

// Re-export the hook and toast function
export { useToast, toast };
export type { ToastOptions };
